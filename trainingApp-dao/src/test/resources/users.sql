DROP TABLE USER;

CREATE TABLE USER(ID INT GENERATED BY DEFAULT AS IDENTITY (START WITH 1, INCREMENT BY 1) PRIMARY KEY,
	EMAIL VARCHAR(100) NOT NULL,
	FIRSTNAME VARCHAR(100)NOT NULL,
	LASTNAME VARCHAR(100)NOT NULL,
	PASSWORD VARCHAR(30) NOT NULL,
	NUMBER INT,
	STREET VARCHAR(30),
	TOWN VARCHAR(30),
	COUNTRY VARCHAR(30) NOT NULL
);

GRANT ALL PRIVILEGES ON USER TO SA;

INSERT INTO USER(EMAIL,FIRSTNAME,LASTNAME,PASSWORD,NUMBER,STREET,TOWN,COUNTRY) VALUES ('test@yahoo.fr','firstName','lastName','test',1,'euler','palavas','FR');

COMMIT;




